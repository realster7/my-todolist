# my-todolist 앱의 최상위 지침

## 반드시 준수해야 할 지침

- 모든 커뮤니케이션은 한국어로 할 것.
- 오버엔지니어링 금지

## 참조 문서

| 문서명 | 경로 | 요약 |
| --- | --- | --- |
| 도메인 정의서 | `docs/1-domain-definition.md` | 도메인 용어, 핵심 엔티티(User/Category/Todo), 엔티티 관계, 할일 상태 판별 규칙, 비즈니스 규칙(BR-01~07), 핵심 유스케이스(UC-01~09), Out of Scope, 미결 이슈(OI-01~03) 정의 |
| PRD | `docs/2-PRD.md` | 목표 사용자(학생/20~50대 직장인), 성공 지표(동시접속 1,000명), 범위·기능 요구사항(UC별 우선순위), 비기능 요구사항, 기술 스택(React 19+TS+Zustand+TanStack Query / Node.js+Express+pg / PostgreSQL 17 / JWT access+refresh), 2일·1인 개발 일정 및 리스크 정의 |
| 사용자 시나리오 | `docs/3-user-scenario.md` | MVP 유스케이스 기반 사용자 시나리오(학생 과제 관리, 직장인 카테고리별 관리, 지연 할일 처리 등) 및 예외/엣지 케이스(토큰 재발급, 소유권 위반, 날짜 위반 등), 시나리오-요구사항 추적 매트릭스 |
| 와이어프레임 | `docs/4-wireframe.md` | MVP 화면(회원가입/로그인/할일 목록·등록·편집/삭제 확인/회원정보 수정)의 데스크톱·모바일 ASCII 와이어프레임 및 공통 컴포넌트(상태 뱃지 등) 규칙 |
| 프로젝트 구조 설계 원칙 | `docs/5-project-principle.md` | 최상위 원칙, 의존성/레이어 원칙(프론트 FSD 구조, 백엔드 Route-Controller-Service-Query), 코드/네이밍 원칙, 테스트/품질 원칙, 설정/보안/운영 원칙, 프론트엔드·백엔드 디렉토리 구조 |
| 아키텍처 다이어그램 | `docs/6-arch.md` | 전체 시스템 구성도(Client-Backend-DB, JWT 흐름), 백엔드 레이어 구조도, 할일 등록(UC-04) 데이터 흐름 시퀀스 다이어그램(mermaid) |
| ERD | `docs/7-erd.md` | User/Category/Todo 테이블의 컬럼·PK/FK·제약사항을 나타낸 mermaid ERD, 테이블별 비즈니스 규칙 요약, 인덱스 대상 컬럼 |
| DDL 스키마 | `docs/schema.sql` | PostgreSQL 17 기준 users/categories/todos 테이블 생성 DDL(제약조건·인덱스 포함) |
| 실행 계획(WBS) | `docs/8-pan.md` | DB/백엔드/프론트엔드 단위로 분할한 실행 Task(수행 작업/체크박스 완료조건/선행 Task), Task-요구사항 추적 매트릭스, 의존성 흐름 |
| API 명세 | `backend/swagger.json` | OpenAPI 3.0 형식의 TodoList API 명세(회원가입/로그인/토큰 재발급/카테고리 조회/할일 CRUD·필터링), JWT 인증 스킴, 요청·응답 스키마 및 에러 코드 정의 |

## 작업 원칙 (Andrej Karpathy 스타일, 출처: andrej-karpathy-skills/CLAUDE.md)

1. **코딩 전에 먼저 생각하기**: 추측하지 말 것. 혼란스러운 부분을 숨기지 말고 드러낼 것. 트레이드오프를 명시적으로 제시할 것. 가정은 명시적으로 말하고, 해석이 여러 개면 전부 제시하고, 더 단순한 접근이 있으면 제안하고, 불확실하면 진행 전에 물어볼 것.
2. **단순함 우선**: 문제를 해결하는 데 필요한 최소한의 코드만 작성. 요청받지 않은 기능, 불필요한 추상화, 요청받지 않은 유연성, 일어날 수 없는 케이스에 대한 에러 처리는 넣지 않는다. 기준: "숙련된 엔지니어가 봤을 때 과하다고 느낄까?"
3. **외과적 변경**: 반드시 수정해야 하는 부분만 건드릴 것. 정리는 자기가 만든 범위 안에서만. 기존 코드 수정 시 기존 스타일을 유지하고, 무관한 개선은 하지 않으며, 이번 변경으로 인해 실제로 안 쓰게 된 import/변수만 제거한다.
4. **목표 지향 실행**: 성공 기준을 정의하고, 검증될 때까지 반복할 것. 작업을 측정 가능한 목표와 명확한 검증 단계로 바꿔서, 매번 확인받지 않고도 독립적으로 진행할 수 있게 한다.

이 원칙들이 잘 지켜지고 있는지의 신호: 불필요한 diff가 줄어드는지, 과설계로 인한 재작성이 줄어드는지, 확인 질문이 구현 이후가 아니라 구현 전에 나오는지.
